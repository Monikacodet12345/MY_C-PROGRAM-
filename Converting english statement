#include <stdio.h>

int main() {
    char name[50];
    int age
    printf(" name: ");
    scanf("%s", name);
    printf("your age: ");
    scanf("%d", &age);
    printf("Hello %s, you are %d years old.\n", name, age);

    return 0;
}
